# Force npm to accept legacy peer deps for this project during CI installs
legacy-peer-deps=true
# Allow installs that require compiling native modules in environments where build tools are available
unsafe-perm=true
# Ensure npm uses the package-lock if present (no effect if absent)
package-lock=true
